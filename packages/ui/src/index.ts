/**
 * @sanbao/ui — React components, hooks, and UI primitives shared across apps.
 *
 * Import individual modules via subpath exports:
 *   import { Button } from '@sanbao/ui/components/ui/Button'
 *   import { useIsMobile } from '@sanbao/ui/hooks/useIsMobile'
 *   import { markdownToDocx } from '@sanbao/ui/lib/export-docx'
 */

export {};
