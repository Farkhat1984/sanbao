/**
 * Re-export all animations from @sanbao/shared.
 * This file exists for backward compatibility — all existing `@/lib/animations` imports continue working.
 * New code should import directly from `@sanbao/shared/animations`.
 */
export * from "@sanbao/shared/animations";
