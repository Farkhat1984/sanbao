/**
 * Re-export all validation schemas from @sanbao/shared.
 * This file exists for backward compatibility — all existing `@/lib/validation` imports continue working.
 * New code should import directly from `@sanbao/shared/validation`.
 */
export * from "@sanbao/shared/validation";
