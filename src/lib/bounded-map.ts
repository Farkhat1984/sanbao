/**
 * Re-export BoundedMap from @sanbao/shared.
 * This file exists for backward compatibility — all existing `@/lib/bounded-map` imports continue working.
 * New code should import directly from `@sanbao/shared/bounded-map`.
 */
export { BoundedMap } from "@sanbao/shared/bounded-map";
