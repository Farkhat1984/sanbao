/**
 * System prompts — no hardcoded prompts.
 * Agent prompts are managed via admin panel (instructions field on Agent model).
 * Global system prompt is managed via SystemSetting (prompt_system_global).
 */
