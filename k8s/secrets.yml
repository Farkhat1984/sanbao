# TEMPLATE — replace values with base64-encoded secrets
# kubectl create secret generic sanbao-secrets --from-env-file=.env -n sanbao
apiVersion: v1
kind: Secret
metadata:
  name: sanbao-secrets
  namespace: sanbao
type: Opaque
data:
  # echo -n "value" | base64
  NEXTAUTH_SECRET: "REPLACE_BASE64"
  DB_PASSWORD: "REPLACE_BASE64"
  REDIS_PASSWORD: "REPLACE_BASE64"
  ADMIN_PASSWORD: "REPLACE_BASE64"
  ENCRYPTION_KEY: "REPLACE_BASE64"
  MOONSHOT_API_KEY: "REPLACE_BASE64"
  DEEPINFRA_API_KEY: "REPLACE_BASE64"
  S3_ACCESS_KEY: "REPLACE_BASE64"
  S3_SECRET_KEY: "REPLACE_BASE64"
  S3_BACKUP_BUCKET: "REPLACE_BASE64"
  S3_ENDPOINT: "REPLACE_BASE64"
  STRIPE_SECRET_KEY: "REPLACE_BASE64"
  STRIPE_WEBHOOK_SECRET: "REPLACE_BASE64"
  GOOGLE_CLIENT_ID: "REPLACE_BASE64"
  GOOGLE_CLIENT_SECRET: "REPLACE_BASE64"
  CRON_SECRET: "REPLACE_BASE64"
  SENTRY_DSN: "REPLACE_BASE64"
